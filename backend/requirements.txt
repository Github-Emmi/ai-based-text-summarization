# Web framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Database
asyncpg>=0.29.0
sqlalchemy[asyncio]>=2.0.0
alembic>=1.12.0
greenlet>=3.0.0

# Config
pydantic[email]>=2.4.0
pydantic-settings>=2.0.0

# Auth
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0

# AI — OpenAI
openai>=1.3.0

# AI — HuggingFace
# torch, transformers, sentence-transformers, keybert not installed locally (Python 3.14 / no wheels)
# HuggingFace Inference API mode used instead; local pipeline enabled via USE_LOCAL_MODEL=true

# PDF
pdfplumber>=0.10.0
reportlab>=4.0.0

# NLP utilities
# langdetect>=1.0.9  # optional: install manually if needed (not required for core functionality)
# keybert>=0.7.0  # optional: requires sentence-transformers which has no Python 3.14 wheels yet

# Rate limiting
slowapi>=0.1.9

# HTTP client (for HF Inference API)
httpx>=0.25.0
